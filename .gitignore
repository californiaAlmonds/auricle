# ── Rust / Cargo ──────────────────────────────────────────────
/target/
**/target/
**/*.rs.bk
*.pdb

# ── Downloaded optional add-ons (yt-dlp / ffmpeg) ─────────────
# These third-party tools are never bundled or committed.
/bin/

# ── Build / log artifacts ─────────────────────────────────────
build_output.txt
*.log

# ── Environment / secrets ─────────────────────────────────────
.env
.env.*

# ── Internal planning docs (not shipped) ──────────────────────
checklist.md
native-checklist.md
rearchitecture.md

# ── Editor / local agent config (not shipped) ─────────────────
.vscode/
.github/agents/

# ── OS / editor cruft ─────────────────────────────────────────
.DS_Store
Thumbs.db
dist/auricle-0.1.3-setup.exe
dist/auricle.exe
