{
  "name": "auricle-desktop",
  "private": true,
  "version": "0.1.1",
  "scripts": {
    "build": "cargo build --bin native_shell",
    "run": "cargo run --bin native_shell",
    "release": "cargo build --bin native_shell --release"
  }
}
