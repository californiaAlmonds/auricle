{
  "name": "auricle-desktop",
  "private": true,
  "version": "0.1.4",
  "description": "Auricle — a native desktop music player for Windows, built with Rust and Slint.",
  "scripts": {
    "build": "cargo build --bin auricle",
    "run": "cargo run --bin auricle",
    "release": "cargo build --bin auricle --release"
  }
}
